# Changelog

Clippy 桌面宠物（likanchen/clippy-pet-desktop）发布记录。

## [v0.2.4] — 2024-09-04（修复与打磨）

### 修复
- **完整进入/退出动画**：启动、退出、皮肤切换保留完整 Greeting/GoodBye 动画，
  不再被 5 秒超时保险截断成简化版（仅保留 30 秒极长兜底防卡死）。
- **开机自启动修复**：打包版（onefile）自启动命令改为直接指向 exe 本身，
  原来误用 `__file__`（指向一次性临时解压目录），注册表值会失效。
- **气泡尾巴对中**：消息气泡/设置对话框的三角尾巴改用 Clippy 实际窗口尺寸
  对中（适配默认 150% 缩放），不再偏离中心。
- **全屏免打扰轮询链可取消**：关闭免打扰或退出程序时取消已排程的检测，
  退出动画期间不再被 `withdraw()` 吞掉。

### 优化
- TR 文案全面清理符号/emoji（菜单、气泡、按钮、番茄钟提示），
  无字体方框、无清洗后残留空格。
- 字体按字号模块级缓存，弹气泡/对话框不再重复加载 TTF。

### 行为不变
- 功能与交互不变（提醒 / 番茄钟 / 快捷键 / 换肤 / 拖拽 / 中英文）。

## [v0.2.3] — 2024-08-28（性能优化，功能不变）

- 启动内存大幅下降：惰性帧缓存 + LRU 上限 200 帧（峰值 ~50MB）。
- 6055 张精灵 PNG 无损压缩：素材体积 -38%。
- 全屏免打扰轮询：前台窗口未变化时跳过检测（消除每 2 秒 Win32 批量调用）。
- 换肤菜单缩略图缓存复用；动画同帧跳过重绘；快捷键轮询 150ms → 250ms。
- 待机动画恢复官方帧率（1.0x）。

## [v0.2.2] — 2024-08-24（onefile 单文件打包）

- 打包从 onedir（文件夹 + `_internal`）改为 **onefile 单 exe**（88MB，
  内嵌全部素材）；`settings.json` 持久化到 exe 旁，单文件可直接运行。
- 启动/退出/皮肤切换使用完整进入退出动画。
- 待机动画与官方 1:1（纯随机 idle，无去重池，官方帧时长）。
- 新增多条随机启动欢迎语。
- 默认缩放 150%；边缘抗锯齿；菜单/气泡无方框。

## [v0.2.1] — 2024-08（待机与体验演进）

- 待机动画重构：随机 idle 播放（先后采用池去重、穿插方案，最终对齐官方）。
- 交互动作播放完回待机；表演动作保留官方 branching/exitBranch 自然收尾。
- 全屏免打扰误判修复：排除桌面壳层/最大化窗口/隐藏窗口，支持多显示器。
- 气泡/设置对话框单图层渲染 + 色键透明。